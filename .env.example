# ============================================================
# 数据库配置 (PostgreSQL - Vercel)
# ============================================================
# 部署到 Vercel 时，在 Vercel 项目中添加 Postgres 数据库后，
# Vercel 会自动提供以下两个环境变量：
#
#   POSTGRES_PRISMA_URL        - 连接池地址（应用运行时使用）
#   POSTGRES_URL_NON_POOLING   - 直连地址（用于 Prisma 迁移）
#
# 本地开发时，可以从 Vercel Dashboard 复制这两个值
# ============================================================
# POSTGRES_PRISMA_URL=
# POSTGRES_URL_NON_POOLING=

# ============================================================
# Clerk 认证配置
# ============================================================
# 1. 访问 https://clerk.com 注册账户
# 2. 创建新应用
# 3. 在 Dashboard > API Keys 获取以下值
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_xxxxx
CLERK_SECRET_KEY=sk_test_xxxxx
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/dashboard
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/dashboard

# Clerk Webhook 密钥（用于同步用户数据）
CLERK_WEBHOOK_SECRET=whsec_xxxxx

# 应用配置
NEXT_PUBLIC_APP_URL=http://localhost:3000
